version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend/rift-rewind
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/rift-rewind
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
